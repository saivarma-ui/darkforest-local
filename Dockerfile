FROM node:16

WORKDIR app
COPY . ./

RUN yarn install

ARG NODE_ENV
ARG DEFAULT_RPC
ARG DEPLOYER_MNEMONIC
ARG HARDHAT_NETWORK
ARG AL_SERVER_HOST
ARG ROUND_START_TIMESTAMP
ARG ROUND_END_TIMESTAMP
RUN ./scripts/ci_build_deploy.sh

EXPOSE 3011

CMD ["yarn", "start:api"]
